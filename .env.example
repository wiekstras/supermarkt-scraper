# PostgreSQL — gedeeld met de Next.js voordeeleter app
DATABASE_URL=postgres://user:password@localhost:5432/voordeeleter?sslmode=disable

# REST API beveiliging
API_KEY=changeme
CRON_SECRET=changeme

# Server poort (standaard 8080)
PORT=8080
